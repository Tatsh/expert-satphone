#import "HostClass.h"

// A plain value object with no methods of its own; every accessor is compiler-synthesised over the
// declared properties, matching the binary's all-synthesised @c HostClass.
@implementation HostClass
@end
