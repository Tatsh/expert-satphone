/**
 * @file
 * A label whose text is inset from the left edge.
 *
 * Reconstructed from Ghidra program Jubeat (class InfoLabel, image base 0x100000000). All
 * @ghidraAddress values are offsets relative to that image base.
 *
 * RECONSTRUCTION STATE: complete. The runtime metadata declares one method for this class and it is
 * implemented.
 *
 * The superclass was read from the class object's superclass slot at 0x350be0, which binds to
 * @c _OBJC_CLASS_$_UILabel at load time rather than being stored in the file.
 */

#import <UIKit/UIKit.h>

NS_ASSUME_NONNULL_BEGIN

/**
 * A @c UILabel that draws its text 12 points in from the left.
 */
@interface InfoLabel : UILabel
@end

NS_ASSUME_NONNULL_END

// code: language=Objective-C
// kate: hl Objective-C;
// vim: set ft=objc :
